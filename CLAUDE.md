@AGENTS.md

<!--
Claude Code reads CLAUDE.md, not AGENTS.md. This one line imports the canonical
constitution so the native loader fires. Add Claude-only notes below if ever needed.
Windows: keep this as an @import — do NOT use a symlink (symlinks need admin there).
-->
