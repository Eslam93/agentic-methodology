# Architecture Map

<!--
TEMPLATE — replace with the real map. In Phase 0 the builder reverse-engineers a v0 from the code
(the reviewer checks it); thereafter keep it current. It's a *current-state sketch*, not exhaustive
docs. Detection signal for the "Architecture" capability: this dir exists and is non-placeholder.
-->

## System map
<!-- the components/modules and how they fit — a diagram or a tight list -->

## Entry points
<!-- where execution starts: CLIs, servers, jobs, handlers -->

## Data stores & external integrations
<!-- DBs, caches, queues, third-party APIs -->

## Boundaries
<!-- frontend/backend split, service boundaries, trust boundaries -->

> This map is *what is*; the **why** lives in `docs/adr/`.
