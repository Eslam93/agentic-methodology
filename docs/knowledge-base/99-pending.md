# Pending

Everything found and not acted on. One line each, written in the same turn, grouped by who can
act. `P0` blocks the current goal · `P1` matters soon · `P2` worth doing · `?` needs a decision.
This is the index of what is open, not the evidence for it.

## 1 · Only the owner can answer these

- `P0` `baseline.sh review` records `review_commit.<checkout>` from `git rev-parse HEAD` with no check for an in-progress git operation, so a review recorded mid-merge produces a false green: measured 2026-09-07, `check` printed "and the code still matches the reviewed commit" and exited 0 both mid-merge and after `git merge --abort`, two materially different trees on the check that carries the Tier 3 contract. Found by the independent review of `3970d4e`, which fixed the same defect at `seal` only.
- `P0` `baseline.sh seal` honours an inherited `GIT_DIR`, because `git -C <dir>` does not override it. Measured 2026-09-07: sealing inside a mid-merge checkout A with `GIT_DIR` pointing at repository C recorded `baseline_commit.<A>` holding C's HEAD, a commit absent from A, and the merge went undetected. Git exports `GIT_DIR` to every hook and to `git bisect run` and `rebase --exec`, so this is reachable without deliberate sabotage. Predates the mid-operation work.

- `?` The standalone `communication-modes` repository still uses persistent-mode wording, and v2 deletes the modes altogether. Delete it, archive it, or leave it. Evidence: `decisions.md` S-08.
- `P2` A review record can still be written into a sealed brief by hand. `seal_sha256` covers the approval fields, `tier` among them, so the tier edit that removed the Tier 3 contract now blocks; the review fields sit outside that digest on purpose, because they are written later in the task. So the load-bearing limit stands: nothing can tell whether a recorded review actually ran. Closing it needs a second digest re-written on each review, or a record git can witness. Evidence: `_investigations/2026-09-05-task-baseline/methodology.md`.
- `?` Whether `baseline.sh check` should also be a `verify.sh` check, so a Tier 3 brief with neither a review nor a waiver reddens the deterministic run and CI rather than only the hand-back. It would need `verify.sh` to know which task is in flight, and it would go red during a normal build, so it was left out of D-19. Evidence: `_investigations/2026-09-05-tier3-review/methodology.md`.
- `?` Whether a per-task allow-list derived from the agreed brief (paths, repositories, the stop-list items) is a kind of rule D-11 admits, or a new decision for `decisions.md`. Evidence: the same page, recommendations table.
- `?` Whether `affaan-m/ECC`, `addyosmani/agent-skills`, `OthmanAdi/planning-with-files`, and `buildermethods/agent-os` join the benchmark set. Evidence: `_readings/harness-benchmark-2026-09-05.md`, the candidate set.
- `?` Whether the large-work and multi-agent axes, rated bottom on 2026-09-05, are declined by design for a solo operator or a gap to close; it decides whether phase briefs in a disposable `working/` folder are a choice or a defect. Evidence: the same page, tiers by dimension.

## 2 · Needs a decision

- `?` Whether `/document` and `/walkthrough` from the fresher installation join the kit. Not shipped in v2.0; add when a real use appears.
- `?` `production-audit` from `ecc` 2.2.1 as an optional review-menu item in production posture. Read, not evaluated.
- `?` Evaluate optional plugin delivery as a transport, later. D-21 answered the question this line used to ask: plugins exist, the stale premise in D-03 is retired, and project-local copy stays canonical on different grounds, because controls should be readable in the project, reviewed in its pull requests, and pinned to its history. What is still open is whether a plugin is worth shipping alongside that, and nobody has asked for one. Evidence: `_investigations/2026-09-05-installer-update/methodology.md`.
- `?` Whether `guard-secrets` should read `MultiEdit` and `NotebookEdit` payload fields; first measure whether the `Edit|Write` matcher fires on them. Evidence: `_readings/external-review-2026-09-05.md`, new finding 3.
- `?` Whether a headless `claude -p` evaluation that bills the subscription is acceptable for one with-versus-without measurement of a skill, the gap in root cause B. Evidence: `_readings/harness-benchmark-takeaways-2026-09-05.md`, section 7.

## 3 · We can do these ourselves

- `P2` `explain` and `summarize` carry an `allowed-tools` list that reads as a restriction but only pre-approves those tools, and `Bash(cat *)` pre-approves a `cat >` overwrite. Drop the lists or say what they are for. Evidence: the front matter of `.claude/skills/explain/SKILL.md` and `.claude/skills/summarize/SKILL.md`, 2026-09-05.
- `P1` Promote or prune the auto-memory notes on the owner's other repositories: 84 notes on one project used as a knowledge base, 6 on the public site including two stale status notes. Evidence: the investigation page.
- `P2` Independent re-verification of the v1 research citations, deferred since 2026-06. Evidence: `_readings/evidence-base.md`.
- `P2` Codex now has its own hooks file format, seen in `ecc` 2.2.1. The secret preflight could run on the Codex side too. Not evaluated.
- `P1` `00-orientation/start-here.md:8, 42` still speak of the v2 plan as unfinished and `index.md:50` still expects a risks page "after Phase 4", although the phases have landed. The README half of this item, twelve of seventeen acceptance tests, was corrected on 2026-09-05 by the drift check built as D-20, which also caught a second stale number in the same sentence. Neither remaining line is mechanically checkable: they are prose about a plan, not a count. Evidence: `_readings/external-review-2026-09-05.md`, R9 and new finding 5.
- `P2` The session binding depends on `CLAUDE_CODE_SESSION_ID`, which the Bash tool carries on this build and which the Claude Code documentation does not list. If it disappears, `baseline.sh seal` says the brief was not bound and both hooks use their fallbacks: visible, but the protection quietly weakens. Re-check on a version change. Evidence: `_investigations/2026-09-05-active-task/methodology.md`, D-18.
- `P2` Nothing retires a task baseline at acceptance, by D-18: a session keeps measuring against its task's commit until the owner agrees the next task. A legitimate test change in later unrelated work in the same session blocks, naming the old brief. Accepted friction until an incident earns a waiver. Evidence: the same page.
- `P2` A finished session leaves `working/active-tasks/<session id>` behind and nothing removes it. Harmless, because another session reads only its own file, and `working/` is disposable. Revisit only if the host offers a reliable session-end event. Evidence: the same page.
- `P2` A subagent runs with its parent's `CLAUDE_CODE_SESSION_ID`, measured 2026-09-05, so a subagent that sealed a brief would move the parent's binding. No skill dispatches a subagent that seals today, and `/codex-relay` is read-only. Revisit before any skill delegates a build. Evidence: the same page.
- `P2` The PowerShell test-file pattern missed a repository-root `tests/` folder while the Bash twin matched it, and the two assertion counters disagreed on words ending in `test` or `it`; both were fixed on 2026-09-05. Nothing in `verify.sh` compares the two shells' patterns, so the next divergence is as quiet as these were. A parity check over the shared regular expressions would catch it. Evidence: the same page.
- `P2` The limit line "No hook sees a browser, an MCP call, chat, or a shared folder" in `standing-orders.md`, `README.md`, and `START-HERE.md` is true of this kit's matchers and false of the platform: a `PreToolUse` matcher can name an MCP tool. Reword to "these hooks". Evidence: `_readings/claude-code-docs-2026-09-05.md`.
- `P2` `verify-on-finish` recognises JavaScript and C# test shapes only: a Go `_test.go` or a pytest file outside `tests/` is not a test to it, and `t.Errorf` never counts. State the limit in the README or widen the patterns. Evidence: `_readings/external-review-2026-09-05.md`, new finding 4.
- `P2` The README describes the hook test suite and the secret scan without two qualifications: 30 cases on Windows against 15 on Linux CI, and `.claude/hooks/` excluded from the scan by design. Evidence: the same page, new findings 6 and 7.
- `P2` The citation check built as D-20 reads only unambiguous syntax, so the 469 backticked spans containing a slash and the 50 backticked hex strings already in this base are not checked at all; only the 32 links and page-relative references are, plus any `repo:` or `commit:` citation written from now on, of which there are none yet. Either migrate the existing citations to the explicit forms, page by page, or accept that the majority of evidence here is checked by a reader only. Evidence: `_investigations/2026-09-05-knowledge-integrity/methodology.md`.
- `?` The page-length rule, 150 to 400 lines, is in both the rules page and the path-scoped rule and is broken by 12 of the 19 durable pages today, so D-20 left it unenforced rather than ship a check that is red on arrival. Split the short pages, or change the rule to match the practice; either way one of the two should move. Evidence: `_investigations/2026-09-05-knowledge-integrity/methodology.md`.
- `P2` `test -e` is case-insensitive on Windows, so a knowledge-base link whose case is wrong passes `knowledge-check.sh` on the owner's machine and fails on Linux. CI on `ubuntu-latest` catches it, one commit after it was written. Comparing against a directory listing would catch it locally. Evidence: the same page.
- `P2` No case in `knowledge-check.test.sh` builds a shape-B workspace, so `repo:` resolution from a workspace root and `commit:` resolution across several checkouts are reasoned about and never run. The same gap D-17 and D-18 recorded for the hooks. Evidence: the same page.
- `?` The reviews of 2026-09-06 were fresh contexts that never saw the reasoning, which is one of the two routes D-19 calls independent, but they were commissioned by the session that wrote the code and none is recorded through `baseline.sh review`, because none of these tasks had a sealed brief. Record them, waive them, or run one the owner commissions. Evidence: `_investigations/2026-09-05-knowledge-integrity/methodology.md`.
- `P2` `seal` writes `working/<task>/pre-existing.txt` and the Stop hook does not read it, so a test the owner had already weakened before the task is attributed to the task: reproduced 2026-09-05 by weakening a test, leaving it dirty, sealing, changing nothing, and watching the hook block naming the task baseline. Consuming the snapshot means deciding what happens when the task later edits one of those same files, which is a design question rather than a patch. Evidence: `_investigations/2026-09-05-task-baseline/methodology.md`.
- `P2` `working/status.md` is one global file while the brief is session-scoped, and `resume-brief` prints the status before the brief, so two concurrent sessions each get their own brief and both see whichever status was written last. D-18 never claimed general concurrency control. Fixing it means session-scoping the status file or dropping it from the resume output. Evidence: the same page.
- `P2` The worktree fix, the second-Stop re-check, and the fail-closed seal branch have cases but have never been seen in a live Claude Desktop session; the suite fires the hooks by hand. Same limit as every other hook behaviour here. Evidence: `_investigations/2026-09-05-task-baseline/methodology.md`.
- `P2` `.claude/worktrees/` was added to the ignore lines the installer writes, on the assumption that adopters who use worktrees will put them there. This repository does not use that path, so the line is untested convention rather than a measured need. Drop it if no adopter wants it. Evidence: the same page.
- `P2` The hook-wiring check needs python on PATH and notes rather than fails without it, so a machine with neither python nor a wired hook gets a note and a green run. Node is already used as a fallback for the settings parse and could be here too. Evidence: `_investigations/2026-09-05-installer-update/methodology.md`.
- `P2` Splitting a test file created during the same task blocks every turn afterwards: the original loses assertions, nothing knows they moved to the new file, and the block cannot be cleared while the session carries that task. Same friction D-17 accepted for merges; found 2026-09-06 by the review. Evidence: `_investigations/2026-09-05-task-baseline/methodology.md`.
- `P2` The added-test rename follow is bounded at five hops, so a sixth chained rename of the same file inside one task falls back to the last commit it found. Nobody renames a file six times in a task, and the bound is there so a cycle cannot hang the hook. Evidence: the same page.
- `?` Nothing kept only in `working/` can be made tamper-proof against the builder that writes there. Task Integrity 1.1 and its review closed each obvious route, and the remaining ones need more than one artefact deleted. Whether the approval should live somewhere git can witness is a design question nobody has asked yet. Evidence: the same page.
- `P2` The updater built as D-21 has never been run by an adopter on a real installation, and its per-state cases run through bash only, although both installers were run end to end and produce identical manifests. A PowerShell twin of `install.test.sh` is the next honest step. Evidence: `_investigations/2026-09-05-installer-update/methodology.md`.
- `P2` `install.sh --update` syncs managed files and the manifest and does not revisit `settings.json`, the ignore and attribute lines, or the knowledge-base skeleton, so a release that adds an ignore line needs a note in its release text and nothing enforces that. Evidence: the same page.
- `?` The README's "four rules" is the one count D-20's drift audit could not take: it means the kit's rules, this repository carries a fifth that is its own project rule, and every install has one under its own name, so no derivation tells them apart. Either mark the project rule in a way the tree can read, or accept that this count stays unchecked. Evidence: `_investigations/2026-09-05-knowledge-integrity/methodology.md`.
- `P2` The PowerShell hooks cannot resolve an MSYS-form `WS_REPOS` path (`/c/Users/...`), which `install.sh` writes into `.workspace` when run from Git Bash, so in shape B on Windows the `.ps1` Stop hook finds no checkouts and exits 0. Convert the form in the hook or write a Windows path from the installer. Found 2026-09-05 by the review of D-17; shape B has still never been run as B.
- `P2` The rationalization note and every fallback note of `verify-on-finish` go to stdout on exit 0, which per the hooks page reaches the debug log only; if a note must reach the assistant it has to become a block or ride on the next turn. Evidence: `_readings/claude-code-docs-2026-09-05.md`.
- `P2` The permissions allow and deny lists in settings: the documentation agent found none on the permissions page while a studied harness ships 22 deny entries in a plugin settings file. Read the settings reference directly before building on either. Evidence: `_readings/claude-code-docs-2026-09-05.md`.

## 4 · Worth doing when someone is in that code anyway

- `P2` The blueprint's `make-launch.sh`, which generates the browser-pane launch profile from the resolved layout, was not brought into the kit. Add it if a shape-B project needs `launch.json`.
- `P2` Two worktree traps measured by GSD, for `working-here.md` when the kit first uses a worktree: `refs/stash` is shared across linked worktrees, and `git clean` in a fresh worktree deletes what a linked checkout still needs. Evidence: `_readings/harness-benchmark-takeaways-2026-09-05.md`, section 8.
- `P2` `is_checkout_root()` compares `pwd` against `cd $(git rev-parse --show-toplevel) && pwd`, which disagrees with itself when a checkout sits under the MSYS `/tmp` mount and is reached by its `/c/...` name: `pwd` answers `/c/Users/<user>/AppData/Local/Temp/...` while the same directory reached through git answers `/tmp/...`, so `baseline.sh` refuses with "git does not report this folder as a checkout root". Measured 2026-09-07 while building the mid-operation refusal; the test suites avoid it because `mktemp -d` already returns a `/tmp/...` path. Harmless for a normal project checkout. It fails for any repository that lives under the Windows temp directory.
